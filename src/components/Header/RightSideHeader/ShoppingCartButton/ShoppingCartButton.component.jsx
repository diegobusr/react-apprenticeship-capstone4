import React from 'react';
import { CustomButton, ContainerDiv } from './ShoppingCartButton.styles';

const ShoppingCartButton = () => {
  return (
    <ContainerDiv>
      <CustomButton />
    </ContainerDiv>
  );
};

export default ShoppingCartButton;
